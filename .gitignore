# Python
venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# OS
.DS_Store

# Data (keep raw + processed small CSVs tracked; ignore large/temp)
data/tmp/

# Runtime user data (accounts, activity log) — never commit
data/users.json
data/user_activity.json

# Models are tracked (small) — comment out next line if they grow large
# models/*.pkl

# Local agent tooling (symlinks into venv)
.agents/
.claude/
